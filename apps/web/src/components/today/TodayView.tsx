'use client';

import { ExecutiveHome } from '@/components/home/ExecutiveHome';

/** @deprecated Use ExecutiveHome — Today and Mission Control are merged at `/`. */
export function TodayView() {
  return <ExecutiveHome />;
}
