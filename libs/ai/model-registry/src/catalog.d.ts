import type { ModelDescriptor } from '@bellasos/contracts';
/**
 * Default model catalog. Cost figures are indicative USD per 1M tokens and are
 * used by the routing engine; they can be overridden at runtime via the model
 * registry / LLM module. Local (Ollama) models are marked privacy-safe.
 */
export declare const DEFAULT_MODELS: ModelDescriptor[];
//# sourceMappingURL=catalog.d.ts.map